<%= render "notifier/partials/headline", {:headline => "Your group has started a new group talk thread."} %>

<p style="line-height: 18px;"><font color="#333333" face="linotype universe, helvetica, arial" size="3">
	<p>A new group talk thread has been created for <%= link_from_email(@group.name, group_url(@group)) %></p>
	<p>&#8220;<%= @post.post %>&#8221;</p>
	<p><%= link_from_email('Click here', group_group_posts_url(@group) ) %>  to view it.</p>
</font></p>