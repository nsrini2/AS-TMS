<%= render "notifier/partials/headline", {:headline => "The last user sync failed"} %>
<p style="line-height: 18px;"><font color="#333333" face="linotype universe, helvetica, arial" size="3">
	The latest user sync attempt failed. The details of the job are below:
	<pre><%=h @user_sync_job.to_xml(:skip_instruct => true, :except => [:data]) %></pre>
</font></p>