<div id="groups">
   <% @category_groups.each do |group| -%>
    <%= render :partial =>'showcase/group', :locals => { :group => group, :classname => cycle('odd', 'even'), :avatar_size => :thumb_small } -%>
  <% end -%>
</div>
