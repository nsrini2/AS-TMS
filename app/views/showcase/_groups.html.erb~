<div id="groups">
   <% @groups.each do |group| -%>
    <%= render :partial =>'showcase/group', :locals => { :group => group, :classname => cycle('odd', 'even'), :avatar_size => :thumb_80 } -%>
  <% end -%>
</div>
