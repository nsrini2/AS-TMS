<div id="ask" class="">				  
  <% events.each_with_index do |event, index| -%>
	<%= render :partial => 'shared/showcase_event_stream_item', :locals => { :event => event.reference, :index => (index + 1) } -%>
  <% end -%>
</div>
