<% content_for(:page_title, 'Travel Market Showcase') -%>
<h2 class="page_title">Travel Market Showcase</h2>
 <div id="travelexpo">
   <div id="travelexpo_marketing_message"><%= marketing_image_tag(@random_marketing_message) -%></div>
    <div class="categories">
     <ul id="expo_categories">
       	<% @sponsor_accounts.each_index do |index| -%>
   	<% account = @sponsor_accounts[index] -%>
        <div class="travelexpo_category">
        <h3><%=account.name%></h3>
        <%=content_tag('li',link_to(image_tag(account.showcase_category_image.public_filename(:medium), :alt => account.name, :width => 170, :height => 220),"http://www.google.com"))%>
        </div>
        <%end%>
      </ul>
   </div>
</div>
