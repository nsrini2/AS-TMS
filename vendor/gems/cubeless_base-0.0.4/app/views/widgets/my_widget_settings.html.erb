<!doctype html>
<html>
<head>
	<link rel="stylesheet" type="text/css" href="/css/screen.css">
	<style type="text/css">
		body { background: #fff; }
			#my_widget_settings { padding: 10px; }
				#my_widget_settings:after { content: '.'; display: block; height: 0; clear: both; visibility: hidden; }
				#my_widget_settings dl { margin: 10px 0; }
					#my_widget_settings dl dt { margin: 0; font-weight: bold; }
					#my_widget_settings dl dd { margin: 0; }
				#my_widget_settings textarea { width: 100%; height: 100px; }
				#my_widget_settings div.buttons {  }
					#my_widget_settings div.buttons input { float: left; margin-right: 5px; }
					#my_widget_settings div.buttons input.restore { float: right; margin-right: 0; width: 120px; }
	</style>
</head>
<body>
	<div id="my_widget_settings">
		<p>Just copy and paste the code for your favorite web widget (usually a &lt;script&gt; tag). Width should be no greater than 250 pixels.</p>
		<%= form_tag my_widget_profiles_path, :method => :post, :id => 'my_widget_settings_form' -%>
			<!-- <%= link_to('restore default', my_widget_profiles_path + '?restore_default=true', :method => :post) -%> -->
			<dl>
				<dt><label for="url">Widget Code</label></dt>
				<dd><%= text_area_tag 'url', @widget -%></dd>
			</dl>
			<div class="buttons">
				<input type="submit" name="commit" value="Save" class="button medium">
				<input type="submit" name="cancel" value="Cancel" class="button medium">
				<input type="submit" name="restore_default" value="Restore Default" class="restore button medium">
			</div>
		</form>
	</div>
</body>
</html>