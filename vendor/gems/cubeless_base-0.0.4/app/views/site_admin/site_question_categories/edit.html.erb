<h2>Editing Question Category</h2>

<% form_for(:site_question_category, @site_question_category, 
						:url => site_admin_site_question_category_path(@site_question_category),
						:html => {:class => "admin_form", :method => :put}, 
						:builder => AdminFormBuilder) do |f| %>

	<%= render :partial => "form", :locals => {:f => f} %>
  
	<div class="buttons">
    <%= f.submit "Update", :class => "button" %>
		<input type="submit" name="reset" value="Cancel" class="button light">
  </div>
<% end %>

