<%= f.error_messages %>

<fieldset>
	<dl class="admin_details">
		<%= f.label :site_profile_question_section_id, "Section" %>
    <%= f.select :site_profile_question_section_id, SiteProfileQuestionSection.find(:all).collect{ |s| [s.name, s.id] } %>
		
  	<%= f.label :label %>
  	<%= f.text_field :label %>

  	<%= f.label :example %>
  	<%= f.text_field :example %>

	  <%= f.check_box_label :completes_profile %>
	  <%= f.check_box :completes_profile %>

	  <%= f.check_box_label :matchable %>
	  <%= f.check_box :matchable %>
	</dl>
</fieldset>
  <%= f.hidden_field :question %>